import { pos } from "./pos.mjs";
import { sprite } from "./sprite.mjs";

class gemObject {
  /**
   *
   * @param {pos} position
   * @param {string} name
   * @param {sprite} sprite
   */
  constructor(position, name, sprite) {}
}
